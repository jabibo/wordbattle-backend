# Place firebase-credentials.json here for local development
# Get from: Firebase Console → Project Settings → Service Accounts → Generate New Private Key
# NEVER commit the credentials file - it's in .gitignore
