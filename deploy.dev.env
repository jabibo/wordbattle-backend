# Development Environment Variables (Self-Hosted)
# Configuration for wordbattle-backend development service
# DO NOT commit this file to git - it's added to .gitignore

# Environment
ENVIRONMENT=development

ADMIN_EMAIL=jan@binge.de
ADMIN_USERNAME=admin

# Database Configuration (Self-Hosted Development)
DB_HOST=wordbattle-db
DB_PORT=5432
DB_NAME=wordbattle_dev
DB_USER=wordbattle_user
DB_PASSWORD=p2n1kqcYFLbx51nsbhUkMYzAHz8oWUGOfwvK3H+okVI=

# Cloud Provider Configuration
CLOUD_PROVIDER=self-hosted

# SMTP Configuration for Email Service - CORRECTED FOR TLS
SMTP_USERNAME=service@binge-wordbattle.de
SMTP_PASSWORD=z1nUNGrz1ZDmu4J
FROM_EMAIL=service@binge-wordbattle.de
SMTP_SERVER=smtp.strato.de
SMTP_PORT=587
SMTP_USE_SSL=false

# Security Configuration (can use weaker key for testing)
SECRET_KEY=09a7f7fbd3bc514c5f51365b58c8055fc00261961ecfe048292dbf81ebcfe44f

# Testing-specific settings
ENABLE_CONTRACT_VALIDATION=true
CONTRACT_VALIDATION_STRICT=false

# Rate limiting (more relaxed for testing)
RATE_LIMIT=60

# Frontend Configuration (development)
FRONTEND_URL=https://wordbattle.binge-dev.de
CORS_ORIGINS=https://wordbattle.binge-dev.de,http://localhost:3000

# Admin Configuration
ADMIN_EMAIL=jan@binge.de 

# AWS Configuration (if needed for multi-cloud testing)
# AWS_ACCESS_KEY_ID=your_aws_access_key_here
# AWS_SECRET_ACCESS_KEY=your_aws_secret_key_here
